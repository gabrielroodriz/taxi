version: '3'

services:
  database:
    image: 'postgres:16.1'

    ports:
      - 5432:5432

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 'password'
      POSTGRES_DB: app

